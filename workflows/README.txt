COMFYUI WORKFLOWS
=================

Place your ComfyUI workflow .json files here. Radix Core uses the active one
(set in Settings -> Image) to render images, and Service Setup -> Sync Assets
can copy these into your ComfyUI install's workflows folder.

Both API-format and frontend/graph-format workflows are accepted; the client
converts graph -> API automatically.
