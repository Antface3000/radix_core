BUNDLED ALLTALK VOICES
======================

Drop the voice files you want Radix Core to install here. They are copied into
your AllTalk install's voices/ directory by Service Setup -> Sync Assets.

Set your AllTalk install path in the Service Setup panel first. Sync is
copy-only and never deletes anything in the target.
