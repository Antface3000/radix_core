# GUI (PySide6 v1)
PySide6>=6.6.0
pillow>=10.0.0

# Writing suite
pyenchant>=3.2.0

# llama-cpp-python is installed by scripts/bootstrap_deps.py (CPU or CUDA wheel)
# so a missing compiler never blocks the writing studio.

# Services: ComfyUI image generation + AllTalk/Piper TTS
requests>=2.31.0
websocket-client>=1.7.0

# Audio playback fallback on non-Windows (Windows uses built-in winsound)
sounddevice>=0.4.6
soundfile>=0.12.1

# Optional: only needed for scripts/download_models.py
huggingface_hub>=0.23.0
